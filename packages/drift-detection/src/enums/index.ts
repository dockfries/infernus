export enum DriftEndReasonEnum {
  TIMEOUT,
  DAMAGED,
  OTHER,
}

export enum DriftStateEnum {
  NONE,
  DRIFTING,
}

export enum DriftOptionsEnum {
  DRIFT_CHECK_ENABLED = 1,
  DAMAGE_CHECK_ENABLED,
}
