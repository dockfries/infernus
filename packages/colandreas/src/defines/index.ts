if (!samp.defined) {
  samp.defined = {};
}

samp.defined._colandreas_included = true;

export const MAX_CA_OBJECTS = 50000;
export const WATER_OBJECT = 20000;
export const FLOAT_INFINITY = 0x7f800000;
export const MAX_MULTICAST_SIZE = 99;

export {};
