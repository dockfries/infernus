export enum MapAndreasMode {
  Minimal,
  /** @deprecated Treated as Minimal. */
  Medium,
  Full,
  /** @deprecated Treated as Full. */
  NoBuffer,
}

export enum MapAndreasError {
  Success,
  InvalidArea,
}
