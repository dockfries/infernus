export enum CefNatives {
  DestroyBrowser,
  HideBrowser,
  AppendToObject,
  RemoveFromObject,
  PlayerHasPlugin,
  ToggleDevTools,
  FocusBrowser,
  AlwaysListenKeys,
}

export enum CefDefault {
  MaxDist = 50.0,
  RefDist = 15.0,
}

export enum CefValueType {
  String,
  Integer,
  Float,
}
